<td><%= schedule.id%></td>
<td><%= schedule.name%></td>
<td><%= schedule.id%></td>
<td>
  <% if schedule.current %>
   	<%= link_to "current schedule", unset_schedule_path(schedule) , method: :put,  class:"btn btn-success", remote: true %>
  <% else %>
   	<%= link_to "current schedule", set_schedule_path(schedule) , method: :put ,class:"btn btn-default", remote: true %>
  <%end%>
    <%= link_to "Show schedule", schedule_path(schedule) , class:"btn btn-info" %>
</td>